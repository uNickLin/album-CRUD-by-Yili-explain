<!-- # <%= form_for @photo, :url => photos_path do |f| %>   :url => photos_path告訴form_for要把東西送到哪，但不給的話就自動回傳到綁定的＠photo頁面上
# 	<%= f.label :title, "Title : " %>
# 	<%= f.text_field :title %>

# 	<%= f.label :date, "Date : " %>
# 	<%= f.text_field :date %>

# 	<%= f.label :description, "Description : " %>
# 	<%= f.text_area :description %>
	
# 	<%= f.label :file_location, "File location : " %>
# 	<%= f.text_field :file_location %>

# 	<%= f.submit "Create" %>

# <% end %> -->

<%= render :partial => "form" %>